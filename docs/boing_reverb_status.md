# Boing branch status

This clean feature branch starts from stable `main` and contains only the BTDR-2/Boing reverb work. It deliberately excludes the old generic-runtime diagnostic overlay and experimental 4x live-path work.
